version: 2

jobs:
  build:
    docker:
      - image: thomasweise/texlive
    steps:
      - checkout
      - setup_remote_docker:
          docker_layer_caching: true
      - run:
          name: Compile
          command: make
      - store_artifacts:
          path: ~/build
